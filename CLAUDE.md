# Working rules for this repo

- After any code change is made in this project, commit it and push to `origin main` right away — do not wait to be asked, and do not batch multiple unrelated changes into a single pending commit. Still follow normal git safety practices: no `--force`, no `--no-verify`, no amending existing commits, and check `git status`/`git diff` before staging broadly.
